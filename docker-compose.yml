version: "3.9"
services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
#  db:
#    image: postgres
#    ports:
#      - "5432:5432"
#    environment:
#      - POSTGRES_USER="user"
